{
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.node.json" }],
  // 仅供 Bun 测试运行器解析 `@/*` 别名（tsc 构建走 references，不读本节）。
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
